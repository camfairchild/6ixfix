{
  "name": "server",
  "version": "1.0.0",
  "description": "back-end server for 6ixFix app",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "setup": "yarn install && cd client && yarn install && cd ..",
    "build-run": "cd client && yarn run build && cd .. && node server.js",
    "start": "nodemon server.js",
    "heroku-postbuild": "cd client && yarn install && yarn run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Affanullah Siddiqui, Joshua Emerson, Cameron Fairchild, Danny Audisho",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cloudinary": "^1.27.1",
    "connect-mongo": "^4.6.0",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "create-react-app": "^4.0.3",
    "cross-env": "^7.0.3",
    "dotenv": "^10.0.0",
    "env-cmd": "^10.1.0",
    "express": "^4.17.1",
    "express-fileupload": "^1.2.1",
    "express-session": "^1.17.2",
    "lodash": "^4.17.21",
    "mongoose": "^6.0.13",
    "validator": "^13.7.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
